import React from 'react'
import { RewardFeature } from '../components/reward-feature'

export default {
  title: 'Example/RewardFeature',
  component: RewardFeature,
}

const Template = (args) => <RewardFeature {...args} />

export const Default = Template.bind({})
