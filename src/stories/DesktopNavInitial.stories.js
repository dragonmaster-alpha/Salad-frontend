import React from 'react'
import { DesktopNavInitial } from '../components/Nav'

export default {
  title: 'Example/Nav',
  component: DesktopNavInitial,
}

const Template = (args) => <DesktopNavInitial {...args} />
export const DesktopNavInitialLayout = Template.bind({})
