import React from 'react'
import { TabletOffScreenNav } from '../components/Nav'

export default {
  title: 'Example/Nav',
  component: TabletOffScreenNav,
}

const Template = (args) => <TabletOffScreenNav {...args} />
export const TabletOffScreenNavLayout = Template.bind({})
