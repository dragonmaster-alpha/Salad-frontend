import React from 'react'
import Social from '../components/Social'

export default {
  title: 'Example/Social',
  component: Social,
}

const Template = (args) => <Social {...args} />
export const SocialLayout = Template.bind({})
