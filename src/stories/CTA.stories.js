import React from 'react'
import { CTA } from '../components/CTA'

export default {
  title: 'Example/CTA',
  component: CTA,
}

const Template = (args) => <CTA {...args} />
export const CTALayout = Template.bind({})
