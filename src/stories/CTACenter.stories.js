import React from 'react'
import { CTACenter } from '../components/CTA'

export default {
  title: 'Example/CTA',
  component: CTACenter,
}

const Template = (args) => <CTACenter {...args} />
export const CTACenterLayout = Template.bind({})
